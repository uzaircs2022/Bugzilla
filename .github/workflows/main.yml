name: PR Review Alert

on:
  issue_comment:
    types: [created]

jobs:
  slack-alert:
    if: github.event.issue.pull_request != null
    runs-on: ubuntu-latest
    steps:
      - name: Extract PR info
        id: pr_info
        run: |
          echo "labels=$(echo '${{ toJson(github.event.pull_request.labels) }}' | jq -r '.[].name' | paste -sd "," -)" >> $GITHUB_ENV
          if [ "${{ github.event.issue.pull_request }}" != "" ]; then
            echo "PR_NUMBER=${{ github.event.issue.number }}" >> $GITHUB_ENV
            echo "PR_TITLE=$(gh pr view ${{ github.event.issue.number }} --json title -q .title)" >> $GITHUB_ENV
            echo "PR_URL=$(gh pr view ${{ github.event.issue.number }} --json url -q .url)" >> $GITHUB_ENV
            # Get labels as a comma-separated string
            echo "PR_LABELS=$(echo '${{ toJson(github.event.pull_request.labels) }}' | jq -r '.[].name' | paste -sd "," -)" >> $GITHUB_ENV
            echo "REPO_NAME=${{ github.repository }}" >> $GITHUB_ENV
            echo "OWNER=${{ github.repository_owner }}" >> $GITHUB_ENV
          else
            echo "This comment is not on a pull request."
            exit 1
          fi

      - name: Send Slack Message
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: C09CPH5KJ3T
          payload: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Title:* ${{ github.event.pull_request.title }}\n*State:* ${{ github.event.review.state }}\n*Labels:* ${{ env.labels }}\n<${{ github.event.pull_request.html_url }}|View PR>"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "action_id": "merge_pr",
                      "text": {
                        "type": "plain_text",
                        "text": "Merge PR",
                        "emoji": true
                      },
                      "style": "primary",
                      "value": "{\"pull_number\":${{ env.PR_NUMBER }},\"repo\":\"Bugzilla\",\"owner\":\"uzaircs2022\"}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

